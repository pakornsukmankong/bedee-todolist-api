getAllTodo => localhost:3000/
getTodoById => localhost:3000/:id 

createTodo => localhost:3000/       body => {title, status, createdAt}
updateTodo => localhost:3000/:id    body => {title, status, createdAt}
deleteTodo => localhost:3000/:id
